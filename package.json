{
  "name": "limit-once",
  "description": "Remember the first result of a function call",
  "author": "Alex Reardon <alexreardon@gmail.com>",
  "keywords": [
    "memoize",
    "memoization",
    "once",
    "cache",
    "performance"
  ],
  "license": "MIT",
  "version": "0.3.0",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/alexreardon/limit-once.git"
  },
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "prepublishOnly": "bun build:all",
    "build:all": "bun build:clean && bun build:dist && bun build:types",
    "build:clean": "rimraf dist",
    "build:dist": "bun build --entry-points ./src/* --outdir ./dist/esm --external '*'",
    "build:types": "tsc ./src/* --outDir ./dist/esm --declaration --emitDeclarationOnly --skipLibCheck",
    "check:prettier": "prettier --debug-check src/**/*.ts test/**/*.ts",
    "check:typescript": "tsc --noEmit ./src/** ./test/** --skipLibCheck --target es2022"
  },
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "exports": {
    ".": "./dist/esm/index.js",
    "./types": "./dist/esm/index.d.ts"
  },
  "files": [
    "/dist",
    "/src"
  ],
  "engines": {
    "bun": ">=1.1.8"
  },
  "dependencies": {
    "bind-event-listener": "^3.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.2.5",
    "rimraf": "^5.0.7",
    "tiny-invariant": "^1.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.4.5"
  }
}
